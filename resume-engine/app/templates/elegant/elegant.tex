\documentclass[letterpaper,9.8pt]{article}
\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage[usenames,dvipsnames]{color}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{tabularx}
\usepackage{array}
\usepackage{fontawesome}
\definecolor{accent}{RGB}{0, 0, 0} % Elegant's cyan accent color

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Adjust margins
\addtolength{\oddsidemargin}{-0.5in}
\addtolength{\evensidemargin}{-0.5in}
\addtolength{\textwidth}{1in}
\addtolength{\topmargin}{-0.7in}
\addtolength{\textheight}{1.35in}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

\titleformat{\section}{
  \vspace{-10pt}\scshape\raggedright\large
}{}{0em}{}[\color{accent}\titlerule\vspace{2pt}] % Using the accent color for the rule

\titlespacing*{\section}{0pt}{18pt}{2pt}

% --- SHARED CUSTOM COMMANDS FOR CONSISTENT SPACING ---
\newcommand{\resumeItem}[1]{\item\small{{#1 \vspace{-3pt}}}}
\newcommand{\resumeSubheading}[4]{
  \vspace{-1pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & #2 \\
      \textit{\small#3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-7pt}
}
\newcommand{\resumeProjectHeading}[2]{
    \item
    \begin{tabular*}{0.97\textwidth}{l@{\extracolsep{\fill}}r}
      \small#1 & #2 \\
    \end{tabular*}\vspace{-7pt}
}
\newcommand{\resumeCertification}[3]{
  \vspace{-1pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & \textit{\small #3} \\
      \textit{\small#2} & \\
    \end{tabular*}\vspace{2pt}
}
\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.1in, itemsep=3pt, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[leftmargin=*, label=$\bullet$]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-6pt}}
% --- END OF CUSTOM COMMANDS ---


\begin{document}

%----------HEADING----------
\BLOCK{ if resume_data.personal_info.fullName }
\begin{center}
    {\Huge \textbf{\VAR{ resume_data.personal_info.fullName | escape_tex }}} \\ \vspace{2pt}
    \small
    \BLOCK{ if resume_data.personal_info.phone }+91-\VAR{ resume_data.personal_info.phone | escape_tex }\BLOCK{ endif }
    \BLOCK{ if resume_data.personal_info.email }\BLOCK{ if resume_data.personal_info.phone } $|$ \BLOCK{ endif }\href{mailto:\VAR{ resume_data.personal_info.email }}{\underline{\VAR{ resume_data.personal_info.email | escape_tex }}}\BLOCK{ endif }
    \BLOCK{ if resume_data.personal_info.address }\BLOCK{ if resume_data.personal_info.email or resume_data.personal_info.phone } $|$ \BLOCK{ endif }\VAR{ resume_data.personal_info.address | escape_tex }\BLOCK{ endif }
    \BLOCK{ if resume_data.personal_info.linkedin }\BLOCK{ if resume_data.personal_info.email or resume_data.personal_info.phone or resume_data.personal_info.address } $|$ \BLOCK{ endif }\href{https://\VAR{resume_data.personal_info.linkedin}}{\faLinkedinSquare\ \underline{LinkedIn}}\BLOCK{ endif }
    \BLOCK{ if resume_data.personal_info.github }\BLOCK{ if resume_data.personal_info.email or resume_data.personal_info.phone or resume_data.personal_info.address or resume_data.personal_info.linkedin } $|$ \BLOCK{ endif }\href{https://\VAR{resume_data.personal_info.github}}{\faGithub\ \underline{GitHub}}\BLOCK{ endif }
    \BLOCK{ if resume_data.personal_info.portfolioUrl }\BLOCK{ if resume_data.personal_info.email or resume_data.personal_info.phone or resume_data.personal_info.address or resume_data.personal_info.linkedin or resume_data.personal_info.github } $|$ \BLOCK{ endif }\href{\VAR{resume_data.personal_info.portfolioUrl}}{\faGlobe\ \underline{Portfolio}}\BLOCK{ endif }
\end{center}
\BLOCK{ endif }

%-----------EDUCATION-----------
\BLOCK{ if resume_data.education and resume_data.education|selectattr('institution')|list|length > 0 }
\section*{Education}
  \resumeSubHeadingListStart
    \BLOCK{ for edu in resume_data.education }
        \BLOCK{ if edu.institution }
        \resumeSubheading
          {\VAR{ edu.institution | escape_tex }}
          {\VAR{ edu.startYear | escape_tex } -- \VAR{ edu.endYear | escape_tex }}
          {\VAR{ edu.degree | escape_tex }}
          {\BLOCK{ if edu.grade }GPA: \VAR{ edu.grade | escape_tex }\BLOCK{ endif }}
        \BLOCK{ endif }
    \BLOCK{ endfor }
  \resumeSubHeadingListEnd
\BLOCK{ endif }

%-----------EXPERIENCE-----------
\BLOCK{ if resume_data.workExperience and resume_data.workExperience|selectattr('company')|list|length > 0 }
\section*{Experience}
  \resumeSubHeadingListStart
    \BLOCK{ for job in resume_data.workExperience }
      \BLOCK{ if job.company }
      \resumeSubheading
        {\VAR{ job.role | escape_tex }}{\VAR{ job.location | escape_tex }}
        {\VAR{ job.company | escape_tex }}{\VAR{ job.startDate | escape_tex } -- \VAR{ job.endDate | escape_tex }}
        \BLOCK{ if job.descriptionPoints }
        \resumeItemListStart
            \BLOCK{ for point in job.descriptionPoints }
                \resumeItem{\VAR{ point | escape_tex }}
            \BLOCK{ endfor }
        \resumeItemListEnd
        \BLOCK{ endif }
      \BLOCK{ endif }
    \BLOCK{ endfor }
  \resumeSubHeadingListEnd
\BLOCK{ endif }

%-----------PROJECTS-----------
\BLOCK{ if resume_data.projects and resume_data.projects|selectattr('projectName')|list|length > 0 }
\section*{Projects}
    \resumeSubHeadingListStart
    \BLOCK{ for proj in resume_data.projects }
      \BLOCK{ if proj.projectName }
      \resumeProjectHeading
          {\textbf{\VAR{ proj.projectName | escape_tex }} $|$ \emph{\VAR{ proj.tech_stack | escape_tex }}}{\VAR{ proj.startDate | escape_tex } -- \VAR{ proj.endDate | escape_tex }}
          \BLOCK{ if proj.descriptionPoints }
          \resumeItemListStart
            \BLOCK{ for point in proj.descriptionPoints }
                \resumeItem{\VAR{ point | escape_tex }}
            \BLOCK{ endfor }
          \resumeItemListEnd
          \BLOCK{ endif }
      \BLOCK{ endif }
    \BLOCK{ endfor }
    \resumeSubHeadingListEnd
\BLOCK{ endif }

%-----------TECHNICAL SKILLS-----------
\BLOCK{ if resume_data.skills and resume_data.skills|selectattr('name')|list|length > 0 }
\section*{Technical Skills}
 \begin{tabularx}{0.97\textwidth}{@{} >{\bfseries}l X @{}}
    \BLOCK{ for skill in resume_data.skills }
        \BLOCK{ if skill.name }
        \VAR{ skill.name | escape_tex }:~ & \VAR{ skill.value | escape_tex } \\
        \BLOCK{ endif }
    \BLOCK{ endfor }
 \end{tabularx}
\BLOCK{ endif }

%-----------ACHIEVEMENTS-----------
\BLOCK{ if resume_data.achievements and resume_data.achievements|selectattr('description')|list|length > 0 }
\section*{Achievements}
\resumeItemListStart
    \BLOCK{ for ach in resume_data.achievements }
        \BLOCK{ if ach.description }
            \resumeItem{\VAR{ ach.description | escape_tex }}
        \BLOCK{ endif }
    \BLOCK{ endfor }
\resumeItemListEnd
\BLOCK{ endif }

%-----------CERTIFICATIONS-----------
\BLOCK{ if resume_data.certifications and resume_data.certifications|selectattr('name')|list|length > 0 }
\section*{Certifications}
\resumeSubHeadingListStart
    \BLOCK{ for cert in resume_data.certifications }
        \BLOCK{ if cert.name }
            \resumeCertification
              {\VAR{ cert.name | escape_tex }}
              {\VAR{ cert.issuer | escape_tex }}
              {\VAR{ cert.date | escape_tex }}
        \BLOCK{ endif }
    \BLOCK{ endfor }
\resumeSubHeadingListEnd
\BLOCK{ endif }

\end{document}